'use strict';

const React = require('react');
const { View } = require('react-native');

const Feather = function MockFeather(props) {
  return React.createElement(View, props);
};

module.exports = { Feather };
